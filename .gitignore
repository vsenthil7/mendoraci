node_modules/
.next/
dist/
build/
coverage/
.turbo/
.cache/
.pnpm-store/

# Environment files — never commit secrets
.env
.env.local
.env.*.local
.env.bob
.env.bob.*
!.env.example
!.env.bob.example

*.log
.DS_Store
Thumbs.db
.idea/
.vscode/
playwright-report/
test-results/
playwright/.cache/
*.tsbuildinfo

# Docker volume mounts (dev-only)
infra/data/

# Evidence pack temp output
.evidence/

# Secrets — never commit
*.pem
*.key

# Local audit backups (FILE BACKUP RULE) — never committed
_backup/
**/_backup/

# Local Claude command logs
logs/ps_cmd_log/

# Demo recordings (large + transient); MP4 added with git add -f
demo/_backup/
demo/thumb_*.png
demo/*.webm
demovideo/.runner/
demovideo/results/creation/latest.txt
tests/playwright/test-results-demo/
.git_commit_msg.txt  
