fix(test): CP-3f bump Playwright image to 1.60.0-jammy + pin matching npm pkg

CP-3e brought up the web container correctly with the new page.tsx (no more
crypto.randomUUID error in this run), but Playwright failed differently:

  browserType.launch: Executable doesn't exist at /ms-playwright/firefox-1522/firefox/firefox
  current: mcr.microsoft.com/playwright:v1.48.0-jammy
  required: mcr.microsoft.com/playwright:v1.60.0-jammy

The pnpm install resolved @playwright/test to 1.60.0 despite the package.json
pin to 1.48.0 — likely because the previous test image had a populated
node_modules baked into a deeper layer and pnpm reconciled to latest within
the test container. Rather than fighting the resolver, accepting that we
track the latest Playwright and updating the docker image to match.

Image: mcr.microsoft.com/playwright:v1.48.0-jammy -> v1.60.0-jammy
package.json: "@playwright/test": "1.48.0" -> "1.60.0"

Expected: 39/39 green this cycle.
